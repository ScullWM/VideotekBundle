{% extends "SwmVideotekBundle:Main:layout.html.twig" %}

{% block body %}
<div class="row">
    <div class="col-md-12">
        <form action="{{ path('video_submit')}}" class="form-horizontal" method="post" {{ form_enctype(form) }}>
            {{ form_widget(form) }}

            <input type="submit" class="btn btn-success" />
        </form>
    </div>
</div>
{% endblock %}